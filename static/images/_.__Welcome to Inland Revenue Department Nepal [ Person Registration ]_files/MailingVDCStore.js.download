Ext.define('MyApp.store.MailingVDCStore', {
    extend: 'Ext.data.Store',

    requires: [
        'MyApp.model.VCDRegCommon_mdl',
        'Ext.data.proxy.Ajax',
        'Ext.data.reader.Json'
    ],

    constructor: function(cfg) {
        var me = this;
        cfg = cfg || {};
        me.callParent([Ext.apply({
            model: 'MyApp.model.VCDRegCommon_mdl',
            storeId: 'MailingVDCStore',
            proxy: {
                type: 'ajax',
                reader: {
                    type: 'json'
                }
            }
        }, cfg)]);
    }
});